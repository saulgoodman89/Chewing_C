#include<stdio.h>
/*
 *
 *	#include를 이용한 헤더 파일 포함.
 *	#include<abc.h> 표준 디렉토리에서 abc.h를 찾아 포함
 *	#include"abc.h" 사용자가 지정한 헤더파일. 
 *
 * 	헤더 파일은 왜 사용하는가 ?
 * 	extren이란 외부 함수를 호출 할 수 있는 키워드를 사용해서 사용 가능하지만 함수가 많아지만 일일히 선언하기 힘듬. 
 */
int main() {

	

}
