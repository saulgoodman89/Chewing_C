
#include<stdio.h>
/*
int pswap(int **pa , int **pb);

int main() {
	int a,b;
	int *pa , *pb;
	printf("pa가 그리키는 변수의 주소값 : %p \n",pa);
	printf("pa의 주소값 : %p \n \n",&pa);
	printf("pb가 가리키는 변수의 주소값 : %p \n",pb);
	printf("pb의 주소값 : %p \n",&pb);

	printf("=== 호출 === \n");
	pswap(&pa,&pb);
	printf("=== 호출 끝 === \n");

	printf("pa가 그리키는 변수의 주소값 : %p \n",pa);
	printf("pa의 주소값 : %p \n \n",&pa);
	printf("pb가 가리키는 변수의 주소값 : %p \n",pb);
	printf("pb의 주소값 : %p \n",&pb);
	 
	 pa가 그리키는 변수의 주소값 : 0x559ca9df00a0 
	 pa의 주소값 : 0x7fff064a4558 
	  
	 pb가 가리키는 변수의 주소값 : 0x7fff064a4660 
	 pb의 주소값 : 0x7fff064a4560 
	 === 호출 === 
	 ppa 가 가리키는 변수의 주소값 : 0x7fff064a4558 
	 ppb 가 가리키는 변수의 주소값 : 0x7fff064a4560 
	 === 호출 끝 === 
	 pa가 그리키는 변수의 주소값 : 0x7fff064a4660 
	 pa의 주소값 : 0x7fff064a4558 
	  
	 pb가 가리키는 변수의 주소값 : 0x559ca9df00a0 
	 pb의 주소값 : 0x7fff064a4560 
	 
}

int pswap(int **ppa, int **ppb) {

	int *temp = *ppa;
	printf("ppa 가 가리키는 변수의 주소값 : %p \n", ppa);
	printf("ppb 가 가리키는 변수의 주소값 : %p \n", ppb);
	*ppa = *ppb;
	*ppb = temp;
	return 0;
}*/

int main() {



}

